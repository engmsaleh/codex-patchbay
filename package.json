{
  "name": "codex-patchbay",
  "version": "0.1.0",
  "description": "Codex-led, evidence-gated delegation to lower-cost coding workers and independent reviewers.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "patchbay": "./dist/cli.mjs"
  },
  "scripts": {
    "build": "bun build ./src/mcp/server.ts --target=node --outfile dist/mcp-server.mjs && bun build ./src/cli.ts --target=node --outfile dist/cli.mjs",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "doctor": "bun run ./src/cli.ts doctor"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
